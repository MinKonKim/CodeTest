#include <string>
#include <vector>
#include <iostream>

using namespace std;


vector<pair<float, float>> ReflectPoints(int startX, int startY, int endX, int endY)
{
	float MidX, MidY;

	vector<pair<float, float>> temp;

	MidX = (startX + endX) / 2;
	MidY = (startY + endY) / 2;

	if (startX == endX)
	{
		if (startY < endY)
		{
			temp.push_back()
		}
	}

}


double Distance(int startX, int startY, int endX, int endY)
{
	double dist1 = endX
}


vector<int> solution(int m, int n, int startX, int startY, vector<vector<int>> a)
{
	vector<int> answer;




	return answer;
}

int main()
{

}